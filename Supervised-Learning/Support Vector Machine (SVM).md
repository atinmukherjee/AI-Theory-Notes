# Support Vector Machine (SVM)
Support Vector Machine (SVM) is a supervised machine learning algorithm primarily used for linear and non-linear classification, although it can also be extended to regression.
